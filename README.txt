Neural Network 
Author: Alex Ameri

  To run, compile the two .java files via javac*.java, and then run NNRunner. You will then be prompted to input the number of input and output nodes, as well as the input patterns and the correct outputs, via the terminal.